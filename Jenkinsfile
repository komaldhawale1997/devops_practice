pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building application wow'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing application'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying application'
            }
        }
    }
}
